naftiko: 1.0.0-alpha2
info:
  label: TIDAL User Collections API — Albums
  description: TIDAL User Collections API — Albums. 5 operations. Self-contained Naftiko capability covering one TIDAL business
    surface.
  tags:
  - TIDAL
  - Music
  - userCollectionAlbums
  created: '2026-05-25'
  modified: '2026-05-25'
binds:
- namespace: env
  keys:
    TIDAL_ACCESS_TOKEN: TIDAL_ACCESS_TOKEN
capability:
  consumes:
  - type: http
    namespace: user-collections-albums
    baseUri: https://openapi.tidal.com
    description: TIDAL User Collections API — Albums business capability. Self-contained, no shared references.
    resources:
    - name: userCollectionAlbums-id
      path: /userCollectionAlbums/{id}
      operations:
      - name: getusercollectionalbums
        method: GET
        description: Get single userCollectionAlbum.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: User collection albums id. Use `me` for the authenticated user's resource
          required: true
        - name: countryCode
          in: query
          type: string
          description: ISO 3166-1 alpha-2 country code
          required: false
        - name: locale
          in: query
          type: string
          description: BCP 47 locale (e.g., en-US, nb-NO, pt-BR). Defaults to en-US if not provided or unsupported.
          required: false
        - name: include
          in: query
          type: array
          description: 'Allows the client to customize which related resources should be returned. Available options: items,
            owners'
          required: false
    - name: userCollectionAlbums-id-relationships-items
      path: /userCollectionAlbums/{id}/relationships/items
      operations:
      - name: deleteusercollectionalbums
        method: DELETE
        description: Delete from items relationship ("to-many").
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: User collection albums id. Use `me` for the authenticated user's resource
          required: true
      - name: getusercollectionalbums
        method: GET
        description: Get items relationship ("to-many").
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: User collection albums id. Use `me` for the authenticated user's resource
          required: true
        - name: page[cursor]
          in: query
          type: string
          description: Server-generated cursor value pointing a certain page of items. Optional, targets first page if not
            specified
          required: false
        - name: sort
          in: query
          type: array
          description: Values prefixed with "-" are sorted descending; values without it are sorted ascending.
          required: false
        - name: countryCode
          in: query
          type: string
          description: ISO 3166-1 alpha-2 country code
          required: false
        - name: locale
          in: query
          type: string
          description: BCP 47 locale (e.g., en-US, nb-NO, pt-BR). Defaults to en-US if not provided or unsupported.
          required: false
        - name: include
          in: query
          type: array
          description: 'Allows the client to customize which related resources should be returned. Available options: items'
          required: false
      - name: postusercollectionalbums
        method: POST
        description: Add to items relationship ("to-many").
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: User collection albums id. Use `me` for the authenticated user's resource
          required: true
        - name: countryCode
          in: query
          type: string
          description: ISO 3166-1 alpha-2 country code
          required: false
        - name: body
          in: body
          type: object
          description: JSON:API document request body.
          required: true
    - name: userCollectionAlbums-id-relationships-owners
      path: /userCollectionAlbums/{id}/relationships/owners
      operations:
      - name: getusercollectionalbums
        method: GET
        description: Get owners relationship ("to-many").
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: id
          in: path
          type: string
          description: User collection albums id. Use `me` for the authenticated user's resource
          required: true
        - name: include
          in: query
          type: array
          description: 'Allows the client to customize which related resources should be returned. Available options: owners'
          required: false
        - name: page[cursor]
          in: query
          type: string
          description: Server-generated cursor value pointing a certain page of items. Optional, targets first page if not
            specified
          required: false
    authentication:
      type: bearer
      value: '{{env.TIDAL_ACCESS_TOKEN}}'
      placement: header
      header: Authorization
      prefix: 'Bearer '
  exposes:
  - type: rest
    namespace: user-collections-albums-rest
    port: 8080
    description: REST adapter for TIDAL User Collections API — Albums. One Spectral-compliant resource per consumed operation,
      prefixed with /v2.
    resources:
    - path: /v2/userCollectionAlbums/{id}
      name: userCollectionAlbums-id
      description: REST surface for userCollectionAlbums-id.
      operations:
      - method: GET
        name: getusercollectionalbums
        description: Get single userCollectionAlbum.
        call: user-collections-albums.getusercollectionalbums
        with:
          id: rest.path.id
          countryCode: rest.query.countryCode
          locale: rest.query.locale
          include: rest.query.include
        outputParameters:
        - type: object
          mapping: $.
    - path: /v2/userCollectionAlbums/{id}/relationships/items
      name: userCollectionAlbums-id-relationships-items
      description: REST surface for userCollectionAlbums-id-relationships-items.
      operations:
      - method: DELETE
        name: deleteusercollectionalbums
        description: Delete from items relationship ("to-many").
        call: user-collections-albums.deleteusercollectionalbums
        with:
          id: rest.path.id
        outputParameters:
        - type: object
          mapping: $.
      - method: GET
        name: getusercollectionalbums
        description: Get items relationship ("to-many").
        call: user-collections-albums.getusercollectionalbums
        with:
          id: rest.path.id
          page[cursor]: rest.query.page[cursor]
          sort: rest.query.sort
          countryCode: rest.query.countryCode
          locale: rest.query.locale
          include: rest.query.include
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: postusercollectionalbums
        description: Add to items relationship ("to-many").
        call: user-collections-albums.postusercollectionalbums
        with:
          id: rest.path.id
          countryCode: rest.query.countryCode
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v2/userCollectionAlbums/{id}/relationships/owners
      name: userCollectionAlbums-id-relationships-owners
      description: REST surface for userCollectionAlbums-id-relationships-owners.
      operations:
      - method: GET
        name: getusercollectionalbums
        description: Get owners relationship ("to-many").
        call: user-collections-albums.getusercollectionalbums
        with:
          id: rest.path.id
          include: rest.query.include
          page[cursor]: rest.query.page[cursor]
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: user-collections-albums-mcp
    port: 9090
    transport: http
    description: MCP adapter for TIDAL User Collections API — Albums. One tool per consumed operation.
    tools:
    - name: tidal-getusercollectionalbums
      description: Get single userCollectionAlbum.
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: user-collections-albums.getusercollectionalbums
      with:
        id: tools.id
        countryCode: tools.countryCode
        locale: tools.locale
        include: tools.include
      outputParameters:
      - type: object
        mapping: $.
    - name: tidal-deleteusercollectionalbums
      description: Delete from items relationship ("to-many").
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: user-collections-albums.deleteusercollectionalbums
      with:
        id: tools.id
      outputParameters:
      - type: object
        mapping: $.
    - name: tidal-getusercollectionalbums
      description: Get items relationship ("to-many").
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: user-collections-albums.getusercollectionalbums
      with:
        id: tools.id
        page[cursor]: tools.page[cursor]
        sort: tools.sort
        countryCode: tools.countryCode
        locale: tools.locale
        include: tools.include
      outputParameters:
      - type: object
        mapping: $.
    - name: tidal-postusercollectionalbums
      description: Add to items relationship ("to-many").
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: user-collections-albums.postusercollectionalbums
      with:
        id: tools.id
        countryCode: tools.countryCode
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: tidal-getusercollectionalbums
      description: Get owners relationship ("to-many").
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: user-collections-albums.getusercollectionalbums
      with:
        id: tools.id
        include: tools.include
        page[cursor]: tools.page[cursor]
      outputParameters:
      - type: object
        mapping: $.
